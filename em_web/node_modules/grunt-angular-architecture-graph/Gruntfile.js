/*
 * grunt-angular-architecture-graph
 * https://github.com/lucalanca/grunt-angular-architecture-graph
 *
 * Copyright (c) 2014 Joao Figueiredo
 * Licensed under the MIT license.
 */

"use strict";

module.exports = function(grunt) {
	// load all grunt tasks matching the `grunt-*` pattern
	require("load-grunt-tasks")(grunt);

	// load configurations within grunt/
	require("load-grunt-config")(grunt);

	grunt.initConfig({
		angular_architecture_graph: {
			diagram: {
				files: {
					'/home/rach/Documentos/TFG/em_repository/em_web/diagram.dot': [ '/home/rach/Documentos/TFG/em_repository/em_web/*' ]
				}
			}
		}
	});

	grunt.loadNpmTasks('grunt-angular-architecture-graph');

	grunt.registerTask('diagram', ['angular_architecture_graph']);
	
	// Actually load this plugin"s task(s).
	grunt.loadTasks("tasks");

};
